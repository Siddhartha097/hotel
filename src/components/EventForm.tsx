import { cn } from "@/lib/utils"
import { EnquireFormProps } from "./EnquireForm"


const EventForm: React.FC<EnquireFormProps> = ({ className }) => {
  return (
    <section className={cn('container', className)}>
        EventForm
    </section>
  )
}

export default EventForm